mode: option_b
num_rounds: 5
local_epochs: 1
batch_size: 64
fedavg_every_round: true
description: Primary label-private SplitFed protocol. Clients send activations only, keep labels local, exchange gradient signals with the server, and participate in FedAvg.
server_url: http://127.0.0.1:8000
auth_token: dev-sfl-token
results_dir: results/label_private_splitfed
